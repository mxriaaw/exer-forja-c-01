#include <stdio.h>

int main(){
    float lado, area;

    printf("digite o valor do lado do quadrado: ");
    scanf("%f", &lado);

        area = lado * lado;

        printf("a area do quadrado e: %.2f\n", area);

        return 0;
}